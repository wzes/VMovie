<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 必须使用此id,drawable为 未选中 状态的图片 -->
    <item
        android:id="@android:id/background"
        android:drawable="@drawable/ic_star_border_black_24dp"/>
    <!-- 必须使用此id,drawable为 半选中 状态的图片 -->
    <item
        android:id="@android:id/secondaryProgress"
        android:drawable="@drawable/ic_star_half_black_24dp"/>
    <!-- 必须使用此id,drawable为 全选中 状态的图片 -->
    <item
        android:id="@android:id/progress"
        android:drawable="@drawable/ic_star_black_24dp"/>
</layer-list>